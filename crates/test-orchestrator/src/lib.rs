//! Public v0.8 test-orchestration surface.
//!
//! The implementation remains split into the dependency-light validation engine so older
//! consumers can migrate without changing durable evidence schemas.

pub use purrcode_validation_runtime::*;
