[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "purrcode-client"
version = "0.1.0"
description = "Typed client for the authenticated PurrCode daemon"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
