# v0.5 usability recovery evidence

Recorded: 2026-07-26T20:10:00-04:00

This report records the release-candidate evidence for Issue #17. It contains no provider
credentials, raw imported secrets, repository content excerpts, or unredacted external responses.

## Phase results

| Phase | Result | Evidence |
|---|---|---|
| 1. Resource control | Passed | Startup fixture made zero generation requests and created no session; lifecycle tests verified unload, keep-alive, idle policy, and one-request low-memory governance. |
| 2. Provider onboarding | Passed | Twenty-five importer tests covered the exact 256 KiB boundary, all supported formats, transient zeroizing secrets, canonical references, and redaction. Forty-two gateway tests covered native Ollama, explicit OpenAI compatibility, structured streaming, bounded diagnostics, and API-mode errors. |
| 3. Streaming | Passed | Agent, daemon, and TUI tests verified real phases, rationale-only deltas, 16–33 ms batching, bounded backpressure, reconnect snapshots, durable-audit separation, stall detection, cancellation, and partial preservation. |
| 4. Recommendation and pull | Passed | Recommendation tests used observed metadata, qualification, memory, swap, and context evidence without model-name quality inference. Pull tests verified exact single-use authorization, executable binding, cancellation, bounded progress, and rediscovery. |
| 5. Governed discovery | Passed | Registry, store, research, MCP, and daemon tests verified installed-qualified-first resolution, avoided external search, DNS-pinned SSRF defenses, immutable downloads, all qualification outcomes, separate install approval, and per-invocation PawGate decisions. |
| 6. Lazy indexing | Passed | Tier 0 remains startup-only, Tier 1 begins after task submission, and daemon-owned bounded Tier 2 pauses for generation, memory pressure, and scheduler latency and terminates on cancellation or failure. |

## Required checks

Executed from the repository root on 2026-07-26:

```text
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

All commands exited successfully. The workspace test run included 275 non-ignored unit and
integration tests plus doc tests. The single environment-gated live Ollama test was run separately.

## Live Ollama acceptance

Environment:

- Ollama 0.4.2 on loopback
- model `llama3.2:1b`
- native `/api/chat` mode

Observed sequence:

1. Provider discovery returned the installed model without generation.
2. Daemon provider configuration performed a real health check and minimal stream.
3. Two provider-backed conversation turns produced non-empty streamed content.
4. The model was explicitly unloaded with `keep_alive: 0`.
5. A final `/api/ps` response was exactly `{"models":[]}`.

Command:

```text
PURRCODE_LIVE_OLLAMA_MODEL=llama3.2:1b cargo test -p purrcode-daemon \
  live_ollama_connect_and_provider_backed_multiturn_streaming -- --ignored --nocapture
```

## Audit notes

- Research evidence retains durable source timestamps; generated reports use explicit ISO 8601
  timestamps with offsets.
- Diagnostic excerpts are bounded and redact authorization tokens, URL credentials, known key
  shapes, and JSON secret fields.
- Skipped or unavailable provider/platform qualification is never represented as passing.
- Migration files remain in the release because NineLives embeds them at compile time.
